import React from 'react';
import TimeTracker from './TimeTracker';


const App = () => {
    return (
        <div className='container'>
            <TimeTracker />
        </div>
    );
};

export default App;
